# This will start the server and print the URL that you need to copy and paste into your web browser.
# Note that if you're port forwarding, you need to replace the port number in the URL with the one that you're forwarding to localhost (your local computer) rather than the one you're running on a lab machine.
jupyter notebook

